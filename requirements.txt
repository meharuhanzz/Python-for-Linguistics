# Python for Linguistics — 5-day tutorial dependencies
# Install with: pip install -r requirements.txt

pandas>=2.0
nltk>=3.8
spacy>=3.7
gensim>=4.3
sentence-transformers>=3.0
transformers>=4.40
torch>=2.0

# Optional, Day 4 deep-dive only (Malayalam FST morphology). Installs cleanly
# via pip; exp2_fst_morphology_malayalam.py falls back to a small hand-built
# demo if you skip this.
mlmorph
